# Delivery Method Reference

The policy for choosing a `DeliveryMethod`, the full current message→channel map, and the
reasoning that should govern any future change.

Read this before touching any `DeliveryMethod` argument.

---

## The rule

> **Reliability is a correctness property, not a performance knob.**
>
> A message may be `Unreliable` **only if a later message supersedes it.** If losing the
> message leaves two machines permanently disagreeing, it must be reliable.

Everything else follows from that one test.

## Classifying a message

Ask, in order:

1. **Is there a next packet that makes this one irrelevant?**
   Positions, rotations, HP totals, absolute XP values — yes. These are *continuous state*.
   The next tick overwrites whatever was lost. → `Unreliable`.

2. **Is it a one-shot state transition?**
   "This enemy died", "this chest was opened", "this weapon was added", "this player started
   charging". There is no next packet. → **Reliable**.

3. **Is it idempotent, and is it re-sent?**
   Idempotent *and* periodically re-sent → `Unreliable` is defensible. Idempotent but sent
   once → still reliable, because "applied zero times" is a divergent state.

4. **Does order matter relative to a sibling message?**
   Paired events (start/stop, spawn/despawn, open/close) must not reorder. → `ReliableOrdered`.
   Independent events with no ordering constraint can use `ReliableUnordered`, which avoids
   head-of-line blocking.

5. **Can the payload exceed MTU?**
   LiteNetLib fragments **reliable** channels. It does **not** fragment unreliable ones —
   anything over ~1400 bytes on an unreliable channel fails to send. If a message carries a
   list, a string, or an inventory snapshot, it must be reliable regardless of the answers
   above. This applies to `OnWeaponAdded` (carries
   `Il2CppSystem.Collections.Generic.List<StatModifier> upgradeOffer`), `OnTomeAdded`,
   `OnChestOpened`, and any future snapshot message.

## Channel semantics

| `LiteNetLib.DeliveryMethod` | Delivered | Ordered | Fragments | Use for |
|---|---|---|---|---|
| `ReliableOrdered` | guaranteed | yes | yes | paired/sequential events; the safe default |
| `ReliableUnordered` | guaranteed | no | yes | independent one-shot events; avoids head-of-line blocking |
| `ReliableSequenced` | latest guaranteed | yes, drops stale | yes | periodic state where only the newest matters |
| `Sequenced` | best effort | drops stale | no | high-rate state where staleness is worse than loss |
| `Unreliable` | best effort | no | **no** | high-rate continuous state, superseded every tick |

---

## Current map (`main` @ `041881b`)

88 send sites across 58 methods in `SynchronizationService.cs`. Generated by scanning for
`DeliveryMethod.*` and attributing each to its enclosing method.

### Unreliable — correct

| Method | Payload | Why it is safe |
|---|---|---|
| `OnEnemyDamaged` | damage tick | Also has a `ReliableOrdered` path; the unreliable one is the high-rate case. Superseded by the next HP sync. |
| `UdpClientService.Update` → local player update | position/rotation | Continuous state, sent every tick |

Note `SynchronizationService.cs:1521` carries `//TODO: Can be unreliable` on a site that is
already `Unreliable` — the TODO is stale, delete it.

### ReliableUnordered — independent one-shots

| Method |
|---|
| `OnFinalBossOrbsSpawned` |
| `OnSpawnedChest` |
| `OnSpawnedEnemy` |
| `OnSpawnedProjectile` |
| `SendSpawnedObject` |
| `OnFinalBossOrbDestroyed` (also has a `ReliableOrdered` path) |
| `OnSelectedCharacter` (also has a `ReliableOrdered` path) |

These are spawn notifications. Each is independent of the others, so ordering is not needed —
but delivery is, because a missed spawn means the entity never exists on that client.

### ReliableOrdered — everything else (49 methods)

<details>
<summary>Full list</summary>

`HandleGameEvent`, `HandleWantToStartFollowingPickup`, `OnChangeGold`, `OnChestOpened`,
`OnEnemyDamaged`, `OnEnemyDied`, `OnEnemyExploder`, `OnFinalBossOrbDestroyed`,
`OnHatChanged`, `OnInteractableFightEnemySpawned`, `OnInteractableUsed`, `OnItemAdded`,
`OnItemRemoved`, `OnLightningStrike`, `OnPickupApplied`, `OnPickupOrbSpawned`,
`OnPickupSpawned`, `OnPlayerDied`, `OnProjectileDone`, `OnReceivedPlayerDied`,
`OnReceivedPlayerDisconnected`, `OnReceivedStartingToChargingLamp`,
`OnReceivedStartingToChargingPylon`, `OnReceivedStartingToChargingShrine`,
`OnReceivedStoppingChargingLamp`, `OnReceivedStoppingChargingPylon`,
`OnReceivedStoppingChargingShrine`, `OnRespawn`, `OnRunStarted`, `OnSelectedCharacter`,
`OnSpawnedEnemySpecialAttack`, `OnSpawnedObjectInCrypt`, `OnStartingToChargingLamp`,
`OnStartingToChargingPylon`, `OnStartingToChargingShrine`, `OnStoppingChargingLamp`,
`OnStoppingChargingPylon`, `OnStoppingChargingShrine`, `OnStormStarted`, `OnStormStopped`,
`OnSwarmEvent`, `OnTimerStarted`, `OnTomeAdded`, `OnTornadoesSpawned`,
`OnTumbleWeedDespawned`, `OnTumbleWeedSpawned`, `OnWantToStartFollowingPickup`,
`OnWeaponAdded`, `OnWeaponToggled`, `PlayerXpAddXp`, `RewardFinished`,
`SendPickupFollowingPlayer`, `SpawnReviver`

</details>

This map is the result of upstream commit `24f5004`
*"fix: update packet delivery methods to improve performance and prevent desync issues"*.
It is deliberate tuning. Treat it as the baseline.

---

## The `Sea-Bass-cmd` downgrade — do not apply

`Sea-Bass-cmd/optimized-netplay` moves 17 of the above to `Unreliable`. Each fails test 1
(nothing supersedes them) and several fail test 5 (MTU).

| Method | Consequence of one dropped packet |
|---|---|
| `OnEnemyDied` | Enemy never dies on peers. Permanent ghost, never despawns, accumulates over the run. |
| `OnChestOpened` | Chest state diverges; contents mismatch. |
| `OnWeaponAdded` | Player permanently missing a weapon on remote clients. Also a likely MTU-crosser. |
| `OnTomeAdded` | Same, for tomes. |
| `OnPickupSpawned` | XP orb never appears on that client. |
| `OnPickupOrbSpawned` | Same. |
| `OnPickupApplied` | XP never granted. Compounds with the shared-XP model into level drift. |
| `OnWantToStartFollowingPickup` | Orphaned pickup. |
| `SendPickupFollowingPlayer` | Orphaned pickup. |
| `OnInteractableUsed` | Interactable state diverges. |
| `OnProjectileDone` | Projectile never cleaned up on peers. Leaked objects. |
| `OnStartingToChargingShrine` | Charge never registers. |
| `OnStoppingChargingShrine` | **Shrine locked forever** — the charger list never empties. |
| `HandleChargingStart` / `HandleChargingStop` (pylon, lamp) | Same, for pylons and graveyard boss lamps. |
| `OnEnemyExploder` | Missed damage event. Tolerable. |
| `OnSpawnedEnemySpecialAttack` | Missed attack. Tolerable. |

Three aggravating factors:

1. **Highest risk exactly where the change claims benefit.** Packet loss scales with send
   rate. At 600 enemies the `OnEnemyDied` volume peaks, so ghost accumulation is worst in the
   scenario the change targets.
2. **`Unreliable` is also unordered.** A `stop` overtaking its `start` leaves a shrine stuck
   in the charging state with no recovery path — the same end state as P0-1, reached a
   different way.
3. **No fragmentation.** See test 5. `OnWeaponAdded` is the clearest exposure.

There is no resend, no sequence number, and no reconciliation snapshot anywhere in this
codebase. Nothing recovers a lost event.

---

## If you actually want the bandwidth

The legitimate routes, roughly in order of payoff:

1. **Widen the delta thresholds.** `EnemyManagerService.cs:42-43` uses
   `POSITION_TRESHOLD = 0.1f` and `YAW_TRESHOLD = 5.0f`. At 600 enemies, raising the position
   threshold cuts the per-tick enemy payload directly, with a visible-smoothness tradeoff you
   can tune. Zero correctness risk.
2. **Quantize the position payload.** Enemy positions are full-precision floats. Multibonk
   sends `short` quantized values — 6 bytes per position instead of 12. Halves the dominant
   payload.
3. **Distance-cull enemy updates.** `bd9518c` already added a `DistanceThrottler`. Extend it:
   enemies far from a given client do not need per-tick updates *for that client*. Requires
   per-peer culling rather than a single broadcast.
4. **Move independent one-shots from `ReliableOrdered` to `ReliableUnordered`.** Removes
   head-of-line blocking without giving up delivery. Safe for anything with no paired sibling
   — spawn notifications, `OnItemAdded`, `OnHatChanged`. **Not** for charging start/stop.
5. **Batch small messages.** Not currently done. Comes free with the Steamworks migration
   (Nagle is on by default). See
   [`../steamworks/01-api-mapping.md`](../steamworks/01-api-mapping.md).

Measure before and after. `UdpClientService` already tracks latency
(`GetLatency(uint connectionId)`); add byte counters per message type before you tune
anything.

---

## Adding a new message

1. Define it under `src/common/Messages/GameNetworkMessages/`.
2. **Bump `Protocol.Version`** — MemoryPack is positional. See fix
   [P1-3](01-critical-fixes.md#p1-3).
3. Run it through tests 1–5 above and record the answer in a comment at the send site.
4. If it is a delta (like `GoldChanged.Amount`), relay it with `SendToAllClientsExcept`, never
   `SendToAllClients` — see fix [P1-1](01-critical-fixes.md#p1-1).
5. Measure the serialized size. If it can approach 1200 bytes, it is reliable-only.

---

## Testing reliability changes

Never on LAN. Local loopback and same-subnet play both run at effectively 0% loss, which is
precisely the condition under which every bug in this document is invisible.

- **Windows:** [clumsy](https://jagt.github.io/clumsy/) — 3% drop, 50 ms lag, 1% duplicate.
- **Linux:** `tc qdisc add dev <iface> root netem loss 3% delay 50ms duplicate 1%`

Reproduce with a 4+ player final swarm, then check for: ghost enemies that never despawn,
shrines that refuse to charge, inventory mismatches between clients, and gold totals that
disagree.
