# CLAUDE.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create `CLAUDE.md` at the repo root — a ~95-line router that carries the invariants which must hold in any session, and points everything else at the existing skills and docs.

**Architecture:** Thin router, not a guide. Three guidance layers already exist: `.claude/skills/` (7 skills, loaded on demand by the UserPromptSubmit hook), `docs/` (10 files, deep and specific), and now CLAUDE.md (loaded unconditionally into every session). CLAUDE.md carries only what must be true when *no skill loaded*; every operational detail stays in the skill or doc that already owns it. Duplication here is the primary failure mode, because a drifted always-loaded file actively misleads.

**Tech Stack:** Markdown. No build step, no test harness.

## Global Constraints

- **Target length ~95 lines.** This file is loaded into every session; length is a running cost. Over ~120 lines, cut content rather than trimming the reasoning that makes a rule stick.
- **No duplication of skill content.** If a fact is already in a skill, CLAUDE.md links to it rather than restating it. Exception: the four Never-list items, which must survive a session where no skill loaded.
- **Every factual claim must be verified against the repo before it is written.** A wrong claim in an always-loaded file is worse than an absent one.
- **Every path and link must resolve** from the repo root at the time of writing.
- **No commits.** The user explicitly declined committing. Leave all changes in the working tree; do not `git add`, `git commit`, or `git tag`.
- **Excluded by design:** naming conventions (lives in the `csharp` skill; cost of a wrong guess is cosmetic) and session teardown (lives in `bepinex` and `il2cpp`; needs more explanation than a Never-list line affords). Do not add these back.

## Verification Model

There is no test harness for prose, and this repo has no test suite at all. The substitute — used as the gate at the end of every task — is a **claim-by-claim check against the source of truth**. Each factual assertion in the file maps to a file in the repo that proves it. A task is done when every claim it introduced has been checked and every path it cites resolves.

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `CLAUDE.md` | Create | The entire deliverable. Six sections, in order: Identity, Layout, Never, Build, Releasing, Where to look. |

No other file is touched. `.claude/` is already current as of this session and needs no change.

### Verified facts this plan asserts

Confirmed during planning; re-confirm if the repo has moved:

| Claim | Source of truth |
|---|---|
| Fork of `Fcornaire/megabonk-together`; origin is `CEOYap/megabonk-together` | `git remote -v`, `docs/README.md` |
| Up to 6 players, P2P, relay fallback for IPv6 / failed IPv4 | `README.md` feature list |
| Three projects, all `net6.0` | `MegabonkTogether.sln`, the three `.csproj` files |
| `src/common` has no Unity reference | `src/common/MegabonkTogether.Common.csproj` |
| Union tags currently run 0–67 | `src/common/Messages/GameNetworkMessages/GameNetworkMessage.cs` |
| Unreliable channels do not fragment; ~1400 byte ceiling | `docs/netplay/02-delivery-method-reference.md` |
| `HasNetplaySessionStarted()` guard pattern | `src/plugin/Patches/OpenChest.cs` |
| `MegabonkPath` fallback is guarded by `Exists()` | `src/plugin/MegabonkTogether.Plugin.csproj` |
| Release touches csproj `<Version>`, `CHANGELOG.toml`, `README.md`; tag triggers CI | `.github/workflows/build-plugin.yml`, `src/plugin/CHANGELOG.toml` |
| Test under 2–5% packet loss, not LAN | `docs/README.md` ground rules |
| No test suite exists | `find . -iname "*test*"` returns no test project |

All eight doc paths cited in the routing table were confirmed to exist.

---

### Task 1: Write the high-stakes half — Identity, Layout, Never

The three sections a session must get right even with zero skills loaded. Rejectable independently: if a Never-list item is wrong or missing its reasoning, this task fails regardless of what Task 2 produces.

**Files:**
- Create: `CLAUDE.md` (sections 1–3 only; Task 2 appends)

**Interfaces:**
- Consumes: nothing.
- Produces: a `CLAUDE.md` whose heading structure Task 2 appends to — `# MegabonkTogether`, then `## Layout`, `## Never`. Task 2 appends `## Build`, `## Releasing`, `## Where to look` at the same heading level.

- [ ] **Step 1: Re-verify the four Never-list claims against source**

Run each, and confirm the stated expectation before writing anything:

```bash
grep -c "MemoryPackUnion" src/common/Messages/GameNetworkMessages/GameNetworkMessage.cs
```
Expected: `68` (tags 0–67). If different, the file is still correct — the plan asserts "append-only", not a specific count. Do not put a count in CLAUDE.md.

```bash
grep -rn "UnityEngine" src/common/ | wc -l
```
Expected: `0`. If non-zero, **stop** — the "no UnityEngine types in src/common" invariant is already violated and the user needs to know before this file asserts it.

```bash
grep -n "HasNetplaySessionStarted" src/plugin/Patches/OpenChest.cs
```
Expected: one match inside `OnTriggerStay_Postfix`.

```bash
grep -n "1400\|fragment" docs/netplay/02-delivery-method-reference.md
```
Expected: matches describing that LiteNetLib fragments reliable channels only.

- [ ] **Step 2: Write sections 1–3**

Create `CLAUDE.md` with exactly this content:

```markdown
# MegabonkTogether

Peer-to-peer multiplayer mod for **Megabonk** (IL2CPP Unity, BepInEx 6). Up to 6 players,
host-authoritative, LiteNetLib UDP + MemoryPack, with a WebSocket matchmaking server that
relays when a direct connection can't be established.

Fork of [`Fcornaire/megabonk-together`](https://github.com/Fcornaire/megabonk-together).
Fork goals, the CONFIRMED / LIKELY / UNVERIFIED status legend, and standing ground rules:
[`docs/README.md`](docs/README.md).

## Layout

| Project | Path | Constraint |
|---|---|---|
| `Common` | `src/common/` | The wire format. **No `UnityEngine` types** — this compiles into the server. |
| `Plugin` | `src/plugin/` | BepInEx 6 IL2CPP plugin. Ships as `MegabonkTogether.dll`. |
| `Server` | `src/server/` | ASP.NET matchmaking + UDP relay. Dockerized. |

Inside the plugin: `Patches/` detect, `Services/` decide, `Scripts/` are injected
MonoBehaviours that tick, `Helpers/` are stateless utilities. A patch that grew logic belongs
in a service.

## Never

**Break the wire format.** `[MemoryPackUnion(N, ...)]` tags in
`src/common/Messages/GameNetworkMessages/GameNetworkMessage.cs` are append-only — never
renumber, reuse, or remove one. MemoryPack serializes positionally and peers on different mod
versions still handshake, so a changed tag corrupts sessions silently rather than failing
loudly. Adding a field to an existing message is the same hazard.

**Downgrade reliability for speed.** `Unreliable` is correct only where a later message
supersedes this one. One-shot transitions — died, opened, added, started, stopped — are always
reliable, because "applied zero times" is a permanently divergent state. Anything carrying a
list, string, or snapshot must be reliable regardless: LiteNetLib fragments reliable channels
only, so past ~1400 bytes an unreliable send silently fails. Policy and the full message map:
[`docs/netplay/02-delivery-method-reference.md`](docs/netplay/02-delivery-method-reference.md).

**Write an unguarded patch.** Every Harmony patch opens with a
`HasNetplaySessionStarted()` check and an ownership check (local player, or `IsHost`). Without
the first, the mod changes singleplayer. Without the second, every peer rebroadcasts the same
event.

**Claim something works.** There is no test suite. Nothing is verified until it has been run
in-game — say that plainly rather than implying coverage. The interop assemblies in
`src/plugin/stripped-libs/` are signature-only stubs with no method bodies, so any behaviour
inferred from a game method's signature is an assumption: mark it **UNVERIFIED**, as
`docs/netplay/01-critical-fixes.md` does. Netcode bugs are mostly invisible at 0% loss — test
at 2–5%, not on LAN.
```

- [ ] **Step 3: Verify every link and path in what you just wrote**

```bash
ls docs/README.md docs/netplay/02-delivery-method-reference.md docs/netplay/01-critical-fixes.md src/common/Messages/GameNetworkMessages/GameNetworkMessage.cs src/plugin/stripped-libs
```
Expected: all exist, no `No such file` output.

- [ ] **Step 4: Check length so far**

```bash
wc -l CLAUDE.md
```
Expected: roughly 50–60 lines. If already over 70, the Never section is too verbose — tighten prose, keep all four items and their reasoning.

Do **not** commit.

---

### Task 2: Write the operational half — Build, Releasing, Where to look

Reference material. Rejectable independently: a wrong command or a broken routing row fails this task without affecting Task 1's invariants.

**Files:**
- Modify: `CLAUDE.md` (append sections 4–6)

**Interfaces:**
- Consumes: the `CLAUDE.md` created in Task 1. Append at the same `##` heading level; do not alter sections 1–3.
- Produces: the complete file for Task 3 to verify.

- [ ] **Step 1: Verify the build claims**

```bash
grep -n "MegabonkPath\|AssemblyPath\|PluginPath" src/plugin/MegabonkTogether.Plugin.csproj
```
Expected: confirms `AssemblyPath` defaults to `stripped-libs`, and the `MegabonkPath` override is gated on `Exists('$(MegabonkPath)/BepInEx/interop')` — this `Exists()` guard is what makes a bad path fall back **silently** instead of erroring.

```bash
grep -n "tags\|VERSION=" .github/workflows/build-plugin.yml | head
```
Expected: confirms the workflow triggers on tag push and derives the version by stripping a leading `v`.

- [ ] **Step 2: Append sections 4–6**

Append exactly this to `CLAUDE.md`:

````markdown
## Build

```bash
dotnet build MegabonkTogether.sln -c Debug
```

Set `MegabonkPath` to a Megabonk install to compile against that install's interop assemblies
and auto-copy Debug output into `BepInEx/plugins/MegabonkTogether`:

```powershell
$env:MegabonkPath = "<path-to>\steamapps\common\Megabonk"
```

**The trap:** if that path doesn't contain `BepInEx/interop`, the csproj's `Exists()` guard
silently falls back to the committed `src/plugin/stripped-libs/`, which may be stale. It does
not error. Check this first when a game type looks wrong or missing. The auto-copy is
Debug-only and is skipped when `CI` or `GITHUB_ACTIONS` is set.

## Releasing

Three files move together, then a tag:

1. `src/plugin/MegabonkTogether.Plugin.csproj` → bump `<Version>`
2. `src/plugin/CHANGELOG.toml` → new `[version."X.Y.Z"]` block. **Shown in-game** — write it
   for a player, not a developer.
3. `README.md` → matching entry in the changelog `<details>` block
4. Tag `X.Y.Z` and push it; `.github/workflows/build-plugin.yml` builds and publishes

Nothing enforces that the tag and `<Version>` agree — a mismatch ships a build whose in-game
version differs from its release. Thunderstore is published separately and does not follow the
GitHub release.

## Where to look

| Working on | Load skill | Then read |
|---|---|---|
| A synced mechanic, a message, a delivery method | `netcode` | [`docs/netplay/02-delivery-method-reference.md`](docs/netplay/02-delivery-method-reference.md) |
| A Harmony patch | `harmony`, `il2cpp` | neighbouring files in `src/plugin/Patches/` |
| Injected MonoBehaviour, Update loop, pooling | `unity`, `il2cpp` | `src/plugin/Scripts/` |
| Plugin startup, config, logging | `bepinex` | `src/plugin/Plugin.cs` |
| Build, references, CI, Thunderstore/Proton | `build-and-release` | [`THUNDERSTORE_BUILD.md`](THUNDERSTORE_BUILD.md), [`docs/PROTON_SETUP.md`](docs/PROTON_SETUP.md) |
| Where a file goes, service and DI conventions | `csharp` | — |
| FPS, stutter, GC | `unity`, `netcode` | [`docs/netplay/04-performance-and-gc.md`](docs/netplay/04-performance-and-gc.md) |
| "What does the game actually do here?" | `il2cpp` | [`docs/reverse-engineering/00-decompilation-guide.md`](docs/reverse-engineering/00-decompilation-guide.md) |
````

- [ ] **Step 3: Verify every path in the routing table resolves**

```bash
ls docs/netplay/02-delivery-method-reference.md docs/netplay/04-performance-and-gc.md docs/reverse-engineering/00-decompilation-guide.md docs/PROTON_SETUP.md THUNDERSTORE_BUILD.md src/plugin/Patches src/plugin/Scripts src/plugin/Plugin.cs
```
Expected: all exist.

- [ ] **Step 4: Verify every named skill exists**

```bash
ls .claude/skills/
```
Expected: `bepinex  build-and-release  csharp  harmony  il2cpp  netcode  unity` — every skill named in the routing table must appear. If `dotnet` or `xunit` still appear, the cleanup `rm` from the previous session hasn't been run; note it but don't block.

Do **not** commit.

---

### Task 3: Whole-file verification

The gate. Rejectable independently: the content can be right while the file as a whole is too long, self-contradictory, or contradicts a skill.

**Files:**
- Modify: `CLAUDE.md` (fixes only)

**Interfaces:**
- Consumes: the complete `CLAUDE.md` from Tasks 1–2.
- Produces: the final file.

- [ ] **Step 1: Length check**

```bash
wc -l CLAUDE.md
```
Expected: 90–110 lines. Over 120, cut whole items — do not strip the reasoning from a Never-list entry to save lines, since the reasoning is what makes the rule survive a plausible argument to violate it.

- [ ] **Step 2: Check for drift against the skills**

Read `.claude/skills/netcode/SKILL.md` and `.claude/skills/harmony/SKILL.md`, and confirm the Never list does not contradict either. They were written in the same session, so agreement is expected — this catches transcription errors, not genuine disagreement.

Specifically confirm: the delivery rule ("Unreliable only if superseded") and the patch preamble (guard, then ownership check) are stated the same way in both places.

- [ ] **Step 3: Confirm the excluded items stayed excluded**

```bash
grep -in "underscore\|camelCase\|teardown\|_camelCase" CLAUDE.md
```
Expected: no output. Naming conventions and session teardown were deliberately left to the skills; if they crept in, remove them.

- [ ] **Step 4: Read the whole file once, aloud-slow**

Check for: a claim you did not verify in Task 1 or 2; a sentence that states a rule without its consequence; any implication that tests exist; any duplicated fact that a skill already owns.

Fix inline.

- [ ] **Step 5: Report to the user**

State: final line count, that no commit was made, and any claim you could not verify. Leave the file in the working tree.

---

## Self-Review

**Spec coverage** — all six approved sections are covered: Identity and Layout and Never in Task 1; Build, Releasing, and Where to look in Task 2. The two deliberate exclusions are enforced by an explicit check in Task 3 Step 3. The "no commit" instruction is a global constraint and is restated at the end of Tasks 1 and 2.

**Placeholder scan** — no TBDs. Both content steps carry the literal markdown to write rather than describing it. `<path-to>` inside the PowerShell example is an intentional user-supplied value, not a plan placeholder.

**Type consistency** — heading levels are consistent between tasks (Task 1 establishes `#` then `##`; Task 2 appends at `##`). Skill names used in the routing table match the directory names verified in Task 2 Step 4. The eight doc paths in the routing table match those confirmed during planning.

**One risk worth naming:** Task 1 Step 1 can halt the plan. If `grep -rn "UnityEngine" src/common/` returns non-zero, an invariant this file is about to assert is already violated in the codebase, and that's a finding for the user rather than something to write around.
